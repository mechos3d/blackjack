to test:

- 'score' method from black_jack_player class (с различным набором карт)

- 'cards' method from black_jack_player class (тестить, что возвращает именно инстансы и именно те)

- 'give_cards' method from black_jack_player class (с парой наборов карт и со случаем, когда в card_deck карт недостаточно - что должно произойти ?)

- 'reset' method for dealer

- 'reset' method for player

- [:money, :stake, :stand].each - все эти сеттеры и геттеры у player'a

- 'take_cards_to_score_17' method for dealer

- 'value' method for card.rb (с различными картами, отразить логику туза)

- 'initialize' for card_deck (то, что колода наполняется, при инициализации и не наполняется, если в редисе уже есть значение

- сеттер и геттер для cards in card_deck.rb

- take(n) method in card_deck - также протестить ситуацию, когда карт в колоде меньше, чем запрошено.


- тесты отдельных action'ов контроллера:
  - инициализируем партию, назначаем все данные вручную, тригерим действие, проверяем результат.

  - (например поставили игроку счет 20, добавили дилеру определенные карты, убрали из колоды все тузы, нажимаем 'hit') - должны получить проигрыш

  - юнит-тесты метода check_win_condition с различными пердусловиями.

  - полный feature-test от-и-до - заранее продуманная партия, колоду в начале задаем вручную, а не рандомную


